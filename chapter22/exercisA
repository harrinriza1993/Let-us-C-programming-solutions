[A] What will be the output of the following program.

a. #include<stdio.h>

   int main()
   {
	enum status{pass, fail, atkt};
	enum status stud1, stud2, stud3;
	
	stud1 = pass;
	stud2 = fail;
	stud3 = atkt;
	
	printf("%d%d%d\n", stud1, stud2, stud3);
	
	return 0;
   }
   
   Output: 012.
   Notes : The Value in enum always starts from 0 and it increases
           according to the number of values in list.


b. #include<stdio.h>

   int main()
   {
	printf("%f\n", (float) ((int) 3.5 / 2));
	printf("%d\n", (int) (((float) 3 / 2) * 3));
	
	return 0;
   }
   
   Output: 1.000000
           4
   Notes : The expression(float) causes the value to be converted from int to 
           float and viceversa.



